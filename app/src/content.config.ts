/**
 * Astro content-collections config kept as a stub: chapter YAML, narrative
 * markdown, UI JSON and the bibliography are loaded at build time by helpers
 * in `src/lib/content/` so we can run zod validation and honesty checks with
 * a single code path. Nothing here so far; the file exists because Astro's
 * config resolver looks for it.
 */
export const collections = {};
